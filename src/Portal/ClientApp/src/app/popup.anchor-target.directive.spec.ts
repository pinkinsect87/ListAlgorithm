import { Popup.AnchorTargetDirective } from './popup.anchor-target.directive';

describe('Popup.AnchorTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new Popup.AnchorTargetDirective();
    expect(directive).toBeTruthy();
  });
});
